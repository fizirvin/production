import { AddModel, RemoveModel, UpdateModel } from './forms'
import Models from './models'
import reducer from './store/reducers'

export default Models
export { AddModel, reducer, RemoveModel, UpdateModel }
