import { AddModel, formReducer, RemoveModel, UpdateModel } from './forms'
import Models from './models'
import reducer from './store/reducers'

export default Models
export { AddModel, formReducer, reducer, RemoveModel, UpdateModel }
