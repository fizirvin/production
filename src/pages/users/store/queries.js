const query = {
  query: `query {
        users{
            _id
            shortCat
            fullUat
            fullCat
            active
            level
            name
        }
    }`
}

export default query
