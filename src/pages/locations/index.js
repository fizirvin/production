import {
  AddLocation,
  formReducer,
  RemoveLocation,
  UpdateLocation
} from './forms'
import Locations from './locations'
import reducer from './store/reducers'

export default Locations
export { AddLocation, formReducer, reducer, RemoveLocation, UpdateLocation }
