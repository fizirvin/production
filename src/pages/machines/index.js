import { AddMachine, formReducer, RemoveMachine, UpdateMachine } from './forms'
import Machines from './machines'
import reducer from './store/reducers'

export default Machines
export { AddMachine, formReducer, reducer, RemoveMachine, UpdateMachine }
